//Represent a chat window
FBMeet.Chat.Window = prototype({

    // Fields

    foo: "initializer",

    // Constructor

    initialize: function(bar){
        if (bar) this.foo = bar;
    }

    // Methods

});